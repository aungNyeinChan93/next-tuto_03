import React from "react";

export const metaData = {
  title: "login",
};

const Login = async () => {
  return (
    <React.Fragment>
      <section>Login</section>
    </React.Fragment>
  );
};

export default Login;
